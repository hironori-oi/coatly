-- =====================================================================
-- 初回 migration プレースホルダ
--
-- 本ファイルは `pnpm db:generate` 実行時に Drizzle Kit が自動生成する。
-- W1 では `src/lib/db/schema.ts` に schema を確定させているため、
-- 実 migration SQL は drizzle-kit が起動した時点で上書き生成される。
--
-- 手動で SQL を書く必要はない。
-- pnpm install → pnpm db:generate で正規の migration が生成される。
-- =====================================================================

-- placeholder（このファイルは drizzle-kit generate で上書きされる）
SELECT 1;
